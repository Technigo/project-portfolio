// This heading will be use like a main for most of component I use h2 becouse h1 can be use only 1 time.
export const SectionHeading = ({ text, className }) => {
  return (
    <h2 className={className}>{text}</h2>
    )

  };