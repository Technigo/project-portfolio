/* eslint-disable react/prop-types */
/*// eslint-disable-next-line no-unused-vars
import React from "react";
import PropTypes from "prop-types";
import { MyWordsCard } from "./MyWordsCard";

const MyWords = ({ mywords }) => {
  return (
    <section>
      {mywords.map((myword, index) => (
        <div key={index}>
          <MyWordsCard mywords={myword} />
        </div>
      ))}
    </section>
  );
};

// Define prop types for MyWords component
MyWords.propTypes = {
  mywords: PropTypes.arrayOf(
    PropTypes.shape({
      image: PropTypes.string.isRequired,
      github: PropTypes.string.isRequired,
      name: PropTypes.string.isRequired,
      description: PropTypes.string.isRequired,
      tags: PropTypes.arrayOf(PropTypes.string).isRequired,
      // Define other properties of each 'mywords' item here
    })
  ).isRequired,
};

export default MyWords;
*/

// eslint-disable-next-line no-unused-vars
/*import React from "react";
import MainTitle from "../../ReusableComponents/Texts/MainTitle";
import { MyWordsCard } from "./MyWordsCard";

export const MyWords = ({ mywords }) => {
  return (
    <section>
      <MainTitle className="main-title" text="My Words" />{" "}
      {MainTitle for section title 
      {mywords.map((myword, index) => (
        <div key={index}>
          <MyWordsCard mywords={myword} />
        </div>
      ))}
    </section>
  );

*/
