// // Component for a reusable normal text. Takes in the prop text from it's parent components.
export const NormalText = ({ text }) => {
    return (
        <p className="normal">{text}</p>
    )
}
